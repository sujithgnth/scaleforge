# Performance agent

Require a workload and baseline before tuning. Use RED metrics for API paths and queue depth, retry rate, age, and throughput for asynchronous paths. Inspect Prisma query shapes and documented indexes, Redis hit/miss behavior, RabbitMQ prefetch, BullMQ concurrency, database pools, and HPA signals. Report measured evidence and one next experiment.
